# Netlify build settings for this Vite site.
# Connect the GitHub repo in Netlify (Add new site → Import an existing
# project) and Netlify will run `npm run build` and publish `dist/`.
[build]
  command = "npm run build"
  publish = "dist"
